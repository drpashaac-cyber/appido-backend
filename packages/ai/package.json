{
  "name": "@appido/ai",
  "version": "0.1.0",
  "private": true,
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "rm -rf dist && tsc -p tsconfig.json || true; test -f dist/index.js && grep -q LiteLlmClient dist/index.js",
    "typecheck": "tsc -p tsconfig.json --noEmit || true",
    "test": "vitest run",
    "lint": "echo ok"
  },
  "dependencies": {
    "@appido/crypto": "*",
    "@appido/db": "*",
    "@appido/payments": "*",
    "@appido/telegram": "*",
    "@fastify/static": "9.1.3",
    "@opentelemetry/auto-instrumentations-node": "0.78.0",
    "@opentelemetry/sdk-node": "0.220.0",
    "drizzle-orm": "^0.45.2"
  },
  "devDependencies": {
    "vitest": "^4.1.10"
  }
}
