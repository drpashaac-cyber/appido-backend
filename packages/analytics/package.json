{
  "name": "@appido/analytics",
  "version": "0.1.0",
  "private": true,
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "echo 'Skipping tests for @appido/analytics' && exit 0",
    "lint": "echo ok"
  },
  "dependencies": {
    "@appido/db": "*",
    "@fastify/static": "10.1.0",
    "@opentelemetry/auto-instrumentations-node": "0.77.0",
    "@opentelemetry/sdk-node": "0.219.0",
    "drizzle-orm": "^0.45.2"
  },
  "devDependencies": {
    "vitest": "^4.1.9"
  }
}
