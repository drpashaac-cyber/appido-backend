{
  "name": "@appido/payments",
  "version": "0.1.0",
  "private": true,
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "echo 'Skipping tests for @appido/payments' && exit 0",
    "lint": "echo ok"
  },
  "dependencies": {
    "@appido/db": "*",
    "@appido/crypto": "*",
    "@appido/telegram": "*",
    "drizzle-orm": "^0.45.2"
  },
  "devDependencies": {
    "vitest": "^2.1.0"
  }
}