{
  "name": "@appido/workers",
  "version": "0.1.0",
  "private": true,
  "main": "dist/main.js",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "dev": "ts-node-dev --respawn --transpile-only src/main.ts",
    "start": "node dist/main.js",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "echo \"(workers) no tests yet\" && exit 0",
    "lint": "echo ok"
  },
  "dependencies": {
    "@appido/config": "*",
    "@appido/db": "*",
    "@appido/types": "*",
    "bullmq": "^5.34.0",
    "ioredis": "^5.4.0",
    "pino": "^9.5.0",
    "@appido/telegram": "*",
    "drizzle-orm": "^0.38.0",
    "@appido/ai": "*",
    "@appido/crypto": "*",
    "@appido/payments": "*"
  },
  "devDependencies": {
    "ts-node-dev": "^2.0.0"
  }
}
