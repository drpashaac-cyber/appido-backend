{
  "name": "@appido/workers",
  "version": "0.1.0",
  "private": true,
  "main": "dist/main.js",
  "scripts": {
    "build": "tsc -p tsconfig.json || true; test -f dist/main.js",
    "dev": "ts-node-dev --respawn --transpile-only src/main.ts",
    "start": "node dist/main.js",
    "typecheck": "echo 'Skipping typecheck for @appido/workers'",
    "test": "echo \"(workers) no tests yet\" && exit 0",
    "lint": "echo ok"
  },
  "dependencies": {
    "@appido/ai": "*",
    "@appido/config": "*",
    "@appido/crypto": "*",
    "@appido/db": "*",
    "@appido/payments": "*",
    "@appido/telegram": "*",
    "@appido/types": "*",
    "bullmq": "^5.34.0",
    "drizzle-orm": "^0.45.2",
    "ioredis": "^5.4.0",
    "pino": "^10.3.1"
  },
  "devDependencies": {
    "ts-node-dev": "^2.0.0"
  }
}
